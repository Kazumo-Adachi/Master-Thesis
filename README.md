# Master Thesis Code

本リポジトリには、修士論文で使用した数値シミュレーションコード、入力ファイル、および代表的な出力ファイルが格納されています。

## 概要

本コードは、土石流の侵食および堆積過程をシミュレーションするためのものです。
本リポジトリは、モデルの検証や比較に用いた実験ケースごとに整理されています。

## リポジトリ構成

```text
Master-Thesis/
├── README.md
├── 田澤実験/
│   ├── txtファイル/
│   ├── expファイル/
│   ├── output江頭/
│   ├── output修正/
│   ├── Egashira.C
│   └── Hotta.C
├── 鈴木実験/
│   ├── txtファイル/
│   ├── 水路形状/
│   ├── output江頭堆積/
│   ├── output修正堆積/
│   ├── output鈴木堆積/
│   ├── output江頭侵食/
│   ├── output修正侵食/
│   ├── output鈴木侵食/
│   ├── D_Egashira.C
│   ├── D_Hotta.C
│   ├── D_Suzuki.C
│   ├── E_Egashira.C
│   ├── E_Hotta.C
│   └── E_Suzuki.C
└── 經隆実験/
    ├── txtファイル/
    ├── 水路形状/
    ├── outputFIX江頭/
    ├── outputFIX修正/
    ├── outputFIX鈴木/
    ├── FIX_Egashira.C/
    ├── FIX_Hotta.C/
    └── FIX_Suzuki.C/
```

## 内容

### `田澤実験/`

水路実験（田澤ら, 2019）の再現解析に使用したソースコード、入力ファイル、実験データ、および代表的な出力ファイルが格納されています。

`txtファイル/`：各変数の内部格子点（i=0以外）の初期条件を与えるファイル

`expファイル/`：実験における堆積形状の時系列データファイル

`output江頭/`：侵食速度式に江頭式を用いた場合の計算結果を格納するファイル

`output修正/`：侵食速度式に修正式を用いた場合の計算結果を格納するファイル

`Egashira.C`：侵食速度式に江頭式を用いた場合の計算コード

`Hotta.C`：侵食速度式に修正式を用いた場合の計算コード

### `鈴木実験/`

鈴木ら（2009）の数値実験の再現解析に使用したソースコード、入力ファイル、実験データ、および代表的な出力ファイルが格納されています。

`txtファイル/`：各変数の内部格子点（i=0以外）の初期条件を与えるファイル

`水路形状/`：水路形状を与えるファイル

`output江頭堆積/`：堆積水路で侵食速度式に江頭式を用いた場合の計算結果を格納するファイル

`output修正堆積/`：堆積水路で侵食速度式に修正式を用いた場合の計算結果を格納するファイル

`output鈴木堆積/`：堆積水路で侵食速度式に鈴木式を用いた場合の計算結果を格納するファイル

`output江頭侵食/`：侵食水路で侵食速度式に江頭式を用いた場合の計算結果を格納するファイル

`output修正侵食/`：侵食水路で侵食速度式に修正式を用いた場合の計算結果を格納するファイル

`output鈴木侵食/`：侵食水路で侵食速度式に鈴木式を用いた場合の計算結果を格納するファイル

`D_Egashira.C`：堆積水路で侵食速度式に江頭式を用いた場合の計算コード

`D_Hotta.C`：堆積水路で侵食速度式に修正式を用いた場合の計算コード

`D_Suzuki.C`：堆積水路で侵食速度式に鈴木式を用いた場合の計算コード

`E_Egashira.C`：侵食水路で侵食速度式に江頭式を用いた場合の計算コード

`E_Hotta.C`：侵食水路で侵食速度式に修正式を用いた場合の計算コード

`E_Suzuki.C`：侵食水路で侵食速度式に鈴木式を用いた場合の計算コード

### `經隆実験/`

經隆ら（2011）の数値実験の再現解析に使用したソースコード、入力ファイル、実験データ、および代表的な出力ファイルが格納されています。

`txtファイル/`：各変数の内部格子点（i=0以外）の初期条件を与えるファイル

`水路形状/`：水路形状を与えるファイル

`outputFIX江頭/`：侵食速度式に江頭式を用いた場合の計算結果を格納するファイル

`outputFIX修正/`：侵食速度式に修正式を用いた場合の計算結果を格納するファイル

`outputFIX鈴木/`：侵食速度式に鈴木式を用いた場合の計算結果を格納するファイル

`FIX_Egashira.C`：侵食速度式に江頭式を用いた場合の計算コード

`FIX_Hotta.C`：侵食速度式に修正式を用いた場合の計算コード

`FIX_Suzuki.C`：侵食速度式に鈴木式を用いた場合の計算コード


## 使用方法

各CソースファイルをCコンパイラを使用してコンパイルしてください。

## 注意事項

大規模な生データファイル、動画、およびすべての出力ファイルは本リポジトリには含まれていません。
本リポジトリには、主要な計算を理解・再現するために必要なソースコード、入力ファイル、および代表的な出力例のみを格納しています。
